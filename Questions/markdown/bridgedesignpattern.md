## Java Exercises 
### Bridge Design Pattern 

    Implement the Bridge pattern to decouple a set of 'Shape' abstractions (e.g., 'Circle', 
    'Square') from their rendering implementation ('Renderer' interface with 
    'VectorRenderer' and 'RasterRenderer' concrete classes). The shapes will hold a 
    reference to a renderer. 
    
    
