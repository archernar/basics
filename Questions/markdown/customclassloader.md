## Java Exercises 
### Custom ClassLoader 

    Implement a custom ClassLoader that loads a class's '.class' file from an encrypted 
    source (e.g., a simple XOR cipher). The ClassLoader should decrypt the byte code 
    on the fly before defining the class in the JVM. 
    
    
