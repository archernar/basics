## Java Exercises 
### Basic Proxy Server 

    Implement a simple TCP proxy. The server listens on a local port, accepts a client, 
    connects to a hardcoded destination server, and then creates two threads to shuttle 
    data back and forth between the client and destination. 
    
    
