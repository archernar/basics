## Java Exercises 
### Greatest Common Divisor (GCD) 

    Implement Euclid's algorithm for finding the greatest common divisor of two positive 
    integers using recursion. The formula is 'gcd(a, b) = gcd(b, a % b)' with the 
    base case 'gcd(a, 0) = a'. 
    
    
