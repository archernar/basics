## Java Exercises 
### Exception Handling in a 'for' Loop 

    Write a loop that iterates over a list of strings, attempting to convert each to 
    an integer. Use a 'try-catch' inside the loop to handle 'NumberFormatException' 
    for individual strings, allowing the loop to continue processing the rest of the 
    list. 
    
    
