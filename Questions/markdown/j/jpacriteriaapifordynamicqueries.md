## Java Exercises 
### JPA Criteria API for Dynamic Queries 

    Create a data access object (DAO) that uses the JPA Criteria API to build queries 
    dynamically. Implement a search method that takes multiple optional filter criteria 
    (e.g., name, department, start date) and constructs the appropriate query at 
    runtime. 
    
    
