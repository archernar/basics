## Java Exercises 
### Distributed Lock with a Database/Redis 

    Implement a basic distributed lock manager using a shared resource like a database 
    table or a Redis key. The 'lock()' method should be atomic and handle contention 
    from multiple processes. The 'unlock()' method should release the lock. Set an 
    expiration time to handle crashes. 
    
    
