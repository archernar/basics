## Java Exercises 
### Compare `replace()` vs `replaceAll()` 

    Explain the difference between `replace(CharSequence, CharSequence)` and `replaceAll(String 
    regex, String replacement)` with an example. 
    
    
