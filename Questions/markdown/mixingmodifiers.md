## Java Exercises 
### Mixing Modifiers 

    Create a class with public, protected, default, and private members. Create another 
    class and show which members are accessible from where (same class, same package, 
    subclass, different package). 
    
    
