## Java Exercises 
### Application of toRadians() - Physics Calculation 

    In a physics formula that requires an angle in radians, use `Math.toRadians()` to 
    convert a user-supplied angle in degrees. 
    
    
