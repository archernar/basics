## Java Exercises 
### Peer-to-Peer Socket Connection 

    Write an application where two instances, running on different machines, can connect 
    to each other. This requires one to act as a temporary server ('ServerSocket') 
    to accept the initial connection from the other. 
    
    
