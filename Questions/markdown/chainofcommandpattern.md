## Java Exercises 
### Chain of Command Pattern 

    Create a 'Handler' class with a 'successor' field and a method 'handleRequest()'. 
    Subclasses override 'handleRequest()' to process a request or, if they can't, call 
    'super.handleRequest()' which passes it to the successor. 
    
    
