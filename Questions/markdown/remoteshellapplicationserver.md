## Java Exercises 
### Remote Shell Application - Server 

    Create a server that accepts a client connection and then reads command strings. 
    For each command, it executes it on the server's operating system and pipes the 
    process output back to the client socket. 
    
    
