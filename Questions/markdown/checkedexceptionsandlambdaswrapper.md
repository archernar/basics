## Java Exercises 
### Checked Exceptions and Lambdas (Wrapper) 

    Show the difficulty of using a method that throws a checked exception inside a lambda 
    (e.g., 'forEach'). Create a wrapper function that takes such a lambda, executes 
    it, and converts any checked exception into an unchecked one. 
    
    
