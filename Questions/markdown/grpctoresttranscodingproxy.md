## Java Exercises 
### gRPC to REST Transcoding Proxy 

    Build a proxy server that sits between a gRPC service and a standard REST/JSON client. 
    The proxy should accept incoming HTTP requests, translate them into gRPC requests, 
    call the backend gRPC service, and then translate the response back into 
    JSON. 
    
    
