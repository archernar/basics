## Java Exercises 
### Socket Keep-Alive 

    For a long-running connection, enable the TCP keep-alive feature using 'socket.setKeepAlive(true)'. 
    Discuss what this option does at the TCP level to detect dead 
    peers. 
    
    
