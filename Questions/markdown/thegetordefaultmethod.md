## Java Exercises 
### The `getOrDefault` Method 

    Use the `getOrDefault()` method to get the value for an existing key. Then, use it 
    to get a value for a non-existent key, providing a default value like 'Not Found'. 
    
    
    
