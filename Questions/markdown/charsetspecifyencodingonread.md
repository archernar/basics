## Java Exercises 
### Charset - Specify Encoding on Read 

    When reading a file with `FileReader` or `Files.newBufferedReader()`, specify the 
    UTF-8 charset to handle special characters correctly. 
    
    
