## Java Exercises 
### Merge Sort Algorithm 

    Implement the Merge Sort algorithm recursively. Create a 'mergeSort(int[] arr)' function 
    that divides the array into halves, recursively sorts them, and then merges 
    them back together. 
    
    
