## Java Exercises 
### TCP Quote of the Day Server 

    Create a server that, upon a client's connection, sends back a random quote from 
    a predefined list and then closes the connection. The client simply connects, reads 
    the quote, and prints it. 
    
    
