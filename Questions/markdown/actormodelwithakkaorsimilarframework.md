## Java Exercises 
### Actor Model with Akka (or similar framework) 

    Build a simple concurrent application using the actor model (e.g., using the Akka 
    framework). Create actors that pass immutable messages to each other to perform 
    a calculation, such as counting words in a set of documents, without using explicit 
    locks. 
    
    
