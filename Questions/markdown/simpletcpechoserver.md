## Java Exercises 
### Simple TCP Echo Server 

    Write a TCP server that listens on a specific port. When a client connects, the server 
    reads a single line of text from the client, echoes it back, and then closes 
    the connection. 
    
    
