## Java Exercises 
### `loadLibrary` vs `load` 

    In comments, explain the difference between `System.load()` (which takes a full path) 
    and `System.loadLibrary()` (which searches a system path). 
    
    
