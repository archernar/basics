## Java Exercises 
### Compare performance of `+` vs `concat()` 

    In a loop, concatenate strings using both `+` and the `concat()` method. Note the 
    performance difference (conceptually). 
    
    
