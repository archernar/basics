## Java Exercises 
### Graceful Server Shutdown 

    Modify the multithreaded server to include a shutdown hook ('Runtime.getRuntime().addShutdownHook'). 
    When the server is shutting down (e.g., via Ctrl+C), it should 
    gracefully close the server socket and all active client connections. 
    
    
