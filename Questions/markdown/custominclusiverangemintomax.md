## Java Exercises 
### Custom Inclusive Range [min to max] 

    Write a program that generates a random integer within a specific inclusive range 
    [min, max], where `min` and `max` are provided by the user. 
    
    
