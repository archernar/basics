## Java Exercises 
### Simple RPC Framework 

    Build a basic Remote Procedure Call (RPC) framework using sockets and object serialization. 
    A client should be able to invoke a method on a remote object (running 
    on a server) as if it were a local object, with the framework handling the network 
    communication and marshalling of arguments. 
    
    
