## Java Exercises 
### nextGaussian() vs uniform distribution 

    Generate 100 numbers with `nextInt(100)` and 100 with `nextGaussian()`. Compare the 
    resulting distributions conceptually. 
    
    
