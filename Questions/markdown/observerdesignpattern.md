## Java Exercises 
### Observer Design Pattern 

    Implement the Observer design pattern where a `Subject` object maintains a list of 
    `Observer` objects and notifies them of state changes. 
    
    
