## Java Exercises 
### `Callable` and `Future` 

    Submit a `Callable` to an `ExecutorService` that returns a string after a short delay. 
    Use the `Future` object to retrieve and print the result. 
    
    
