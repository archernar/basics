## Java Exercises 
### nextGaussian() Distribution Check 

    Use `nextGaussian()` a million times and count how many results fall into different 
    ranges (e.g., &lt-1, -1 to 0, 0 to 1, &gt1) to verify the normal distribution. 
    
    
    
