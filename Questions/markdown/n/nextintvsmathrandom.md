## Java Exercises 
### nextInt() vs Math.random() 

    Write a short program that achieves the same result (e.g., a dice roll) using both 
    `random.nextInt(6)` and `(int)(Math.random() * 6)`. 
    
    
