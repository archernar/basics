## Java Exercises 
### No Override for Private Methods 

    Create a 'Parent' class with a 'private' method 'secret()'. Create a 'Child' class 
    that defines its own 'secret()' method. Show that this is not an override by calling 
    the method through a 'Parent' reference to a 'Child' object. 
    
    
