## Java Exercises 
### NIO-Based Asynchronous Server 

    Implement a non-blocking TCP server using 'java.nio.channels' (e.g., 'ServerSocketChannel', 
    'Selector'). The server should be able to handle multiple client connections 
    concurrently using a single thread that reacts to I/O events. 
    
    
