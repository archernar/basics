## Java Exercises 
### nextFloat() vs nextDouble() Precision 

    Generate a `float` with `nextFloat()` and a `double` with `nextDouble()`. Print both 
    to show the difference in precision. 
    
    
