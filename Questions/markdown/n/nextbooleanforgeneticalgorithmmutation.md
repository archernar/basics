## Java Exercises 
### nextBoolean() for genetic algorithm mutation 

    In a genetic algorithm, iterate through a chromosome and use `nextBoolean()` to decide 
    whether to mutate each gene. 
    
    
