## Java Exercises 
### Polymorphism in Action 

    Create a `Shape` reference variable. Assign a `new Circle()` object to it and call 
    the `draw()` method to demonstrate polymorphism. 
    
    
