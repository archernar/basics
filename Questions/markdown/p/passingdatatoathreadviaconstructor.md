## Java Exercises 
### Passing Data to a Thread via Constructor 

    Create a 'Runnable' class that accepts data (e.g., a string message) through its 
    constructor and stores it in a private field. The 'run()' method should then use 
    this data. 
    
    
