## Java Exercises 
### Producer-Consumer with 'wait()' and 'notify()' 

    Implement the classic Producer-Consumer problem. Use a shared list (the buffer), 
    a 'Producer' thread that adds items, and a 'Consumer' thread that removes them. 
    Use 'synchronized', 'wait()', and 'notify()' for coordination. 
    
    
