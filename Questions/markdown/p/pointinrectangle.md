## Java Exercises 
### PointInRectangle 

    Check if a point (x, y) is inside a rectangle defined by (x1, y1) and (x2, y2). The 
    condition would be 'x &gt x1 && x &lt x2 && y &gt y1 && y &lt y2'. 
    
    
