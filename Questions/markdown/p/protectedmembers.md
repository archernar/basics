## Java Exercises 
### Protected Members 

    Define a class `BankAccount` with a `protected double balance`. Create a subclass 
    `SavingsAccount` that can directly access and modify the `balance`. 
    
    
