## Java Exercises 
### PolymorphicArray 

    Create an array of 'Vehicle' type that holds both 'Vehicle' and 'Car' objects. Iterate 
    through the array and call the 'drive()' method on each element. 
    
    
