## Java Exercises 
### Pitfall - nextInt() then nextLine() 

    Demonstrate the common pitfall where a call to `nextLine()` after `nextInt()` consumes 
    the newline character. Show how to fix it. 
    
    
