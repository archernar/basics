## Java Exercises 
### Passing Properties via Command Line 

    Run a Java program from the command line with the `-D` flag (e.g., `-Dmykey=myvalue`) 
    and retrieve it using `System.getProperty()`. 
    
    
