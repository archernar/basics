## Java Exercises 
### Parse a Large XML File with SAX 

    Write a program that parses a very large XML document (e.g., &gt1GB) without loading 
    the entire file into memory. Use the SAX (Simple API for XML) parser to process 
    the document as a stream of events. 
    
    
