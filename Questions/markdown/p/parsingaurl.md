## Java Exercises 
### Parsing a URL 

    Write a program that takes a full URL string as input and uses the 'java.net.URL' 
    class to parse and print its individual components: protocol, host, port, path, 
    query, and fragment. 
    
    
