## Java Exercises 
### Depth of a Binary Tree 

    Write a recursive function 'maxDepth(TreeNode root)' that computes the maximum depth 
    (or height) of a binary tree. The depth is the number of nodes along the longest 
    path from the root down to the farthest leaf node. 
    
    
