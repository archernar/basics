## Java Exercises 
### Bytecode Engineering with ASM/ByteBuddy 

    Use a library like ASM or ByteBuddy to dynamically modify the bytecode of a class 
    at runtime. For example, add a timing interceptor to a method that measures its 
    execution time without altering its source code. 
    
    
