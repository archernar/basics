## Java Exercises 
### Mediator Pattern for a GUI 

    Refactor a complex GUI dialog where components (e.g., buttons, text fields, lists) 
    communicate directly with each other. Introduce a 'Mediator' object that centralizes 
    communication, decoupling the components. 
    
    
