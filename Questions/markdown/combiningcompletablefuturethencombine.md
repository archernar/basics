## Java Exercises 
### Combining `CompletableFuture` (`thenCombine`) 

    Create two independent `CompletableFuture` tasks and use `thenCombine` to run a third 
    task only after both are complete. 
    
    
