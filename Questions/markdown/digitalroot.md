## Java Exercises 
### Digital Root 

    The digital root of a number is the single-digit value obtained by an iterative process 
    of summing digits. Write a recursive function 'digitalRoot(int n)' that calculates 
    this. If the sum is more than one digit, the function should call itself 
    with the new sum. 
    
    
