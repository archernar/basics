## Java Exercises 
### Checking if Empty (`isEmpty`) 

    Create a new, empty HashMap and use the `isEmpty()` method to print whether it is 
    empty. Then add an element and check again. 
    
    
