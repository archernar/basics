## Java Exercises 
### Parsing Data from a File 

    Write a program to read a file where each line should contain a comma-separated pair 
    of numbers. Handle 'FileNotFoundException', 'NumberFormatException' if a part 
    is not a number, and 'ArrayIndexOutOfBoundsException' if a line doesn't have two 
    parts. 
    
    
