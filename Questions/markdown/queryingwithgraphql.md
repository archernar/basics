## Java Exercises 
### Querying with GraphQL 

    Set up a GraphQL server (e.g., using 'graphql-java'). Define a schema and create 
    data fetchers. Then, write a Java client that sends complex queries to the server, 
    demonstrating the ability to request precisely the data it needs. 
    
    
