## Java Exercises 
### `identityHashCode` stability 

    Show that `System.identityHashCode(obj)` returns the same value for the lifetime 
    of the object, even if `hashCode()` is overridden. 
    
    
