## Java Exercises 
### Using Data Streams for Primitives 

    Modify the file transfer application to first send the file's size as a 'long' and 
    its name as a 'String' using 'DataOutputStream'. The client should use 'DataInputStream' 
    to read this metadata before receiving the file. 
    
    
