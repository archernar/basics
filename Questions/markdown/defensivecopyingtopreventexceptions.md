## Java Exercises 
### Defensive Copying to Prevent Exceptions 

    Create a class that holds a mutable object (e.g., a 'Date'). In a getter method, 
    return a defensive copy of the object instead of the original reference to prevent 
    external code from causing exceptions by modifying the object's internal state. 
    
    
    
