## Java Exercises 
### Basic Range [0 to n-1] 

    Using `Random.nextInt(n)`, write a program to generate a random integer between 0 
    (inclusive) and a user-defined upper bound `n` (exclusive). 
    
    
