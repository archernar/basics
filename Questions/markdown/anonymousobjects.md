## Java Exercises 
### Anonymous Objects 

    Call a method that requires an object as a parameter by creating an anonymous object 
    directly in the method call, e.g. 'printDetails(new Person( 
    
    
