## Java Exercises 
### Checking Port Availability 

    Write a utility function 'isPortAvailable(int port)' that checks if a given TCP port 
    is free. It should try to bind a 'ServerSocket' to that port; if it succeeds, 
    the port is available (be sure to close it immediately). 
    
    
