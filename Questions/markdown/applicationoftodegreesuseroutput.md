## Java Exercises 
### Application of toDegrees() - User Output 

    After a calculation results in an angle in radians, use `Math.toDegrees()` to show 
    the result to the user in a more readable format. 
    
    
