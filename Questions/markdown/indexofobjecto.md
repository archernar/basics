## Java Exercises 
### indexOf(Object o) 

    Create an `ArrayList` with duplicate string values and use `indexOf()` to find the 
    index of the *first* occurrence of a specific string. 
    
    
