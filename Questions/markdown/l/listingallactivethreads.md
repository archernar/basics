## Java Exercises 
### Listing All Active Threads 

    Write a program that uses 'Thread.getAllStackTraces().keySet()' to get a set of all 
    currently active threads in the JVM. Iterate through the set and print the name, 
    state, and priority of each thread. 
    
    
