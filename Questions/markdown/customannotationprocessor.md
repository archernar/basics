## Java Exercises 
### Custom Annotation Processor 

    Write a custom annotation processor that runs at compile time. Define an annotation 
    like '@Immutable' and have the processor check that any class annotated with 
    it has only final fields and no setter methods, issuing a compiler error if the 
    rules are violated. 
    
    
