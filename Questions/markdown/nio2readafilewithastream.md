## Java Exercises 
### NIO.2 - Read a File with a Stream 

    Use `Files.lines()` to get a `Stream&ltString&gt` of a file's lines and use stream 
    operations (like `filter` or `map`) on it. 
    
    
