## Java Exercises 
### Overriding with Varargs 

    Create a 'Calculator' class with a method 'sum(int... numbers)'. Create a 'LoggingCalculator' 
    subclass that overrides the 'sum' method to log the operation before 
    calling the superclass's method to perform the calculation. 
    
    
