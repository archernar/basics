## Java Exercises 
### Overriding the `toString()` Method 

    Create a `Book` class with fields for title and author. Override the `toString()` 
    method from the `Object` class to return a formatted string. 
    
    
