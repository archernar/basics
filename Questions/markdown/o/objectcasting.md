## Java Exercises 
### Object Casting 

    Create a `Shape` object that is actually a `Circle`. Use `instanceof` to check its 
    type and then downcast it to a `Circle` reference to access Circle-specific methods. 
    
    
    
