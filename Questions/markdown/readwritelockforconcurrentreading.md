## Java Exercises 
### ReadWriteLock for Concurrent Reading 

    Implement a data structure that is read frequently but written to infrequently. Use 
    a 'java.util.concurrent.locks.ReadWriteLock' to allow multiple reader threads 
    to access the data concurrently, while writer threads still have exclusive access. 
    
    
    
