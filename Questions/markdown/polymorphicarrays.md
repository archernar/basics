## Java Exercises 
### Polymorphic Arrays 

    Create an array of type `Shape`. Store objects of `Circle`, `Rectangle`, and `Triangle` 
    in it and loop through to call their overridden `draw()` methods. 
    
    
