## Java Exercises 
### Generating Valid Parentheses 

    Write a recursive function that generates all combinations of n pairs of well-formed 
    parentheses. For n=3, the output should be '((()))', '(()())', '(())()', '()(())', 
    '()()()'. 
    
    
