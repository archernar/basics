## Java Exercises 
### The 'yield()' Method 

    Create two threads that print messages. In the run loop of one thread, call 'Thread.yield()'. 
    Discuss that 'yield()' is a hint to the scheduler to run another thread, 
    but its behavior is not guaranteed. 
    
    
