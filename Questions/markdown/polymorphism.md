## Java Exercises 
### Polymorphism 

    Create a `Shape` class and subclasses `Circle` and `Square`. Create an array of `Shape` 
    objects and call an overridden `draw()` method on each. 
    
    
