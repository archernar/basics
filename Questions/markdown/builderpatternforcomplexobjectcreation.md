## Java Exercises 
### Builder Pattern for Complex Object Creation 

    Implement the Builder pattern for a complex 'HttpClient' class. The builder should 
    allow for configuring parameters like URL, method (GET/POST), headers, connection 
    timeout, and request body in a fluent, readable manner before creating the immutable 
    client object. 
    
    
