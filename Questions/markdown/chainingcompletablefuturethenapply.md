## Java Exercises 
### Chaining `CompletableFuture` (`thenApply`) 

    Chain a `CompletableFuture` with `thenApply` to take the result of one asynchronous 
    task and apply a transformation to it. 
    
    
