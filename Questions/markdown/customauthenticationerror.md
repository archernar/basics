## Java Exercises 
### Custom Authentication Error 

    Create a custom checked exception class named `InvalidCredentialsException`. Then, 
    write a `LoginService` class with a method `void login(String username, String 
    password) throws InvalidCredentialsException`. This method should check if the 
    username is 
    
    
