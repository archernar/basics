## Java Exercises 
### Simple Animation with a Thread 

    Using a simple graphics library like Swing, create a component that has a ball. Use 
    a separate thread to continuously update the ball's position and call 'repaint()', 
    creating a simple animation. (Add comments about Swing's single-thread rule). 
    
    
    
