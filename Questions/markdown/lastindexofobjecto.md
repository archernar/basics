## Java Exercises 
### lastIndexOf(Object o) 

    Create an `ArrayList` with duplicate string values and use `lastIndexOf()` to find 
    the index of the *last* occurrence of a specific string. 
    
    
