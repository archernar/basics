## Java Exercises 
### Simple Base and Derived Class 

    Create a base class `Animal` with a method `eat()`. Create a derived class `Dog` 
    that inherits from `Animal` and calls the `eat()` method. 
    
    
