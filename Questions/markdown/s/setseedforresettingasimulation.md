## Java Exercises 
### setSeed() for Resetting a Simulation 

    Create a simulation that uses a `Random` object. Use `setSeed()` to allow the user 
    to replay the exact same simulation sequence. 
    
    
