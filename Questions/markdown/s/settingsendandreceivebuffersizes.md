## Java Exercises 
### Setting Send and Receive Buffer Sizes 

    Use 'socket.setSendBufferSize()' and 'socket.setReceiveBufferSize()' to manually 
    adjust the underlying OS socket buffer sizes. Discuss the performance implications 
    of changing these values. 
    
    
