## Java Exercises 
### Simple Proxy Server 

    Implement a basic forwarding TCP proxy server. It should listen on one port, accept 
    a client connection, then connect to a predefined target server. It must then 
    shuttle data back and forth between the client and the target server. 
    
    
