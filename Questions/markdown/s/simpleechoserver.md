## Java Exercises 
### Simple Echo Server 

    Create a server that accepts a client connection, reads a single line of text from 
    the client's 'InputStream', writes that same line back to the client's 'OutputStream', 
    and then closes the socket. 
    
    
