## Java Exercises 
### Setting a Date 

    Create a 'CalendarEvent' class. Implement three overloaded 'setDate' methods: one 
    that takes a 'java.util.Date' object, one that takes three integers (year, month, 
    day), and one that takes a 'long' representing milliseconds since epoch. 
    
    
