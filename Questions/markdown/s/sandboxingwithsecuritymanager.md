## Java Exercises 
### Sandboxing with Security Manager 

    Create a Java application that loads and executes untrusted code (e.g., a simple 
    script from a file). Configure and enable the 'java.lang.SecurityManager' with a 
    custom policy file to restrict the untrusted code's permissions, such as preventing 
    file system access or network connections. 
    
    
