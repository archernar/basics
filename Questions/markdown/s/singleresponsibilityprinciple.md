## Java Exercises 
### Single Responsibility Principle 

    Create a `Worker` class with a `work()` method. Create subclasses `HumanWorker` and 
    `RobotWorker` to show how inheritance supports different implementations. 
    
    
