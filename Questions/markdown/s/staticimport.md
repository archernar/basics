## Java Exercises 
### Static Import 

    Use a static import to import all static members of the 'java.lang.Math' class, so 
    you can call methods like 'sqrt()' and 'pow()' directly without the 'Math.' prefix. 
    
    
    
