## Java Exercises 
### SecureRandom vs Random methods 

    Create a `SecureRandom` instance and call its `nextInt()` method. Note that the method 
    signature is the same as in `Random`. 
    
    
