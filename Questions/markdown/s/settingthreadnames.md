## Java Exercises 
### Setting Thread Names 

    Create and start five threads. In the constructor of your 'Runnable', use 'Thread.currentThread().setName()' 
    to give each thread a unique name (e.g., 'Worker-1', 
    'Worker-2'). The 'run()' method should print the thread's name. 
    
    
