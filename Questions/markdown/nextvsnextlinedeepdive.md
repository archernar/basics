## Java Exercises 
### `next()` vs `nextLine()` - Deep Dive 

    Create a string 'Hello World\nNext Line' and show the different outputs when reading 
    it with `next()` twice vs. `nextLine()` once. 
    
    
