## Java Exercises 
### When to Use Static 

    Create two scenarios: one where an instance variable is appropriate (e.g. a car's 
    color) and one where a static variable is appropriate (e.g. the number of wheels 
    on a car, which is always 4). 
    
    
