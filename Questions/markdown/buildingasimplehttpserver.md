## Java Exercises 
### Building a Simple HTTP Server 

    Write a basic web server using 'ServerSocket'. It should listen on a port (e.g., 
    8080), accept incoming connections, parse the first line of the HTTP request to 
    find the requested file path, and serve static files from a local directory. Handle 
    basic '404 Not Found' errors. 
    
    
