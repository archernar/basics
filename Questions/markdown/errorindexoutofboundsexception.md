## Java Exercises 
### Error - IndexOutOfBoundsException 

    Write code that intentionally causes an `IndexOutOfBoundsException` by trying to 
    `get()` an element at an invalid index. 
    
    
