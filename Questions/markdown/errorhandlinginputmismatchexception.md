## Java Exercises 
### Error Handling - InputMismatchException 

    Write a program that prompts for an integer but uses a `try-catch` block to handle 
    an `InputMismatchException` if the user enters text. 
    
    
