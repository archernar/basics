## Java Exercises 
### Distinguish Instance Variables 

    In a constructor for a 'Person' class, use 'this' to assign parameter values (e.g. 
    'name', 'age') to instance variables with the same names (this.name = name). 
    
    
