## Java Exercises 
### Application of getProperty() - OS Detection 

    Use `System.getProperty("os.name")` to detect the underlying operating system and 
    print a different message for Windows vs. Linux. 
    
    
