## Java Exercises 
### Mutual Recursion 

    Implement two functions, 'isEven(int n)' and 'isOdd(int n)', that are mutually recursive. 
    'isEven' should call 'isOdd(n-1)' and 'isOdd' should call 'isEven(n-1)'. 
    Define the base cases appropriately. 
    
    
