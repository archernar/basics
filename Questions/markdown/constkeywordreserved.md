## Java Exercises 
### `const` Keyword (Reserved) 

    Write a comment explaining that `const` is a reserved keyword in Java but is not 
    used, and that `final` is the correct alternative. 
    
    
