## Java Exercises 
### Clearing the HashMap (`clear`) 

    Add several elements to a HashMap, print its size, then use the `clear()` method 
    to remove all entries. Print the size again. 
    
    
