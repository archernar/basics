## Java Exercises 
### Scanner - Custom Delimiter 

    Use a `Scanner`'s `useDelimiter()` method to parse a file that uses a character other 
    than whitespace as a separator (e.g., a semicolon). 
    
    
