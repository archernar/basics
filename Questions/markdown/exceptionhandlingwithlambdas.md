## Java Exercises 
### Exception Handling with Lambdas 

    Create a list of strings, some of which are not valid integers. Use a stream and 
    a lambda expression in the 'map' function to convert them to integers. Handle the 
    'NumberFormatException' within the lambda, perhaps by returning a default value 
    like 0. 
    
    
