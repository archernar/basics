## Java Exercises 
### Recursive Tree Flattening 

    Given the root of a binary tree, flatten it into a 'linked list' in-place using pre-order 
    traversal. The resulting structure should have each node's right child 
    pointing to the next node in the pre-order sequence and its left child as null. 
    
    
    
