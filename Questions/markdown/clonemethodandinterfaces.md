## Java Exercises 
### Clone Method and Interfaces 

    Create a 'CustomCloneable' interface that extends 'Cloneable'. Create an abstract 
    class 'Shape' that implements 'CustomCloneable' and defines a public abstract 'clone()' 
    method. Subclasses like 'Circle' must now override 'clone()'. 
    
    
