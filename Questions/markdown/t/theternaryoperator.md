## Java Exercises 
### The Ternary Operator 

    Use the ternary operator to assign a value to a string variable based on a condition 
    (e.g. `status = (score &gt 60) ? 'Pass' : 'Fail';`). 
    
    
