## Java Exercises 
### Thread Interrupts vs. a 'volatile' Flag 

    Create two versions of a cancellable task. One uses 'Thread.interrupt()' to signal 
    cancellation. The other uses a 'volatile boolean' flag. Discuss the pros and cons 
    of each approach, especially how interrupts handle blocking calls. 
    
    
