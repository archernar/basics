## Java Exercises 
### Traffic Light Simulator 

    Create an `enum` named `TrafficLight` with three possible values: `RED`, `YELLOW`, 
    and `GREEN`. Each enum constant should have an associated String representing 
    the action to take (e.g., RED -&gt 
    
    
