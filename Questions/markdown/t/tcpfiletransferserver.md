## Java Exercises 
### TCP File Transfer - Server 

    Write a server that listens for a connection, receives a file name from the client, 
    and then sends the contents of that file to the client using binary streams ('FileInputStream' 
    and 'OutputStream'). 
    
    
