## Java Exercises 
### Thread States 

    Write a program that creates a new thread. Print its state ('getState()') at various 
    points: after creation (NEW), after 'start()' (RUNNABLE), while it is sleeping 
    (TIMED_WAITING), and after it has finished (TERMINATED). 
    
    
