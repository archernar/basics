## Java Exercises 
### The `putIfAbsent` Method 

    Use `putIfAbsent()` to try and add an entry with a key that already exists. Then, 
    use it to add an entry with a new key. Observe the difference. 
    
    
