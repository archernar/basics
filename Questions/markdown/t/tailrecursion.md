## Java Exercises 
### Tail Recursion 

    Rewrite the factorial function to be tail-recursive. The function 'factorial(int 
    n, int accumulator)' should pass its result in the parameters of the next recursive 
    call. Explain why this form is optimizable. 
    
    
