## Java Exercises 
### Reading until a Sentinel Value 

    Use a `while(scanner.hasNextInt())` loop to read integers until the user enters a 
    non-integer sentinel value (like 'quit'). 
    
    
