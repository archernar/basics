## Java Exercises 
### Reflection-based Object-to-JSON Serializer 

    Write a utility that takes any Java object and serializes it into a JSON string using 
    reflection. The utility should inspect the object's fields (including those 
    of its superclass) and represent them as key-value pairs. Handle primitive types, 
    strings, and nested objects. 
    
    
