## Java Exercises 
### Remote Shell Server 

    Create a server that, upon connection, accepts commands from a client, executes them 
    on the server's machine using 'Runtime.getRuntime().exec()', and sends the command's 
    output back to the client. 
    
    
