## Java Exercises 
### Retrieving Data from a Thread 

    Design a worker thread that computes a result. The 'Runnable' should store the result 
    in a field. The main thread should start the worker, 'join()' it, and then 
    call a getter method on the 'Runnable' instance to retrieve the result after the 
    thread has finished. 
    
    
