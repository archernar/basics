## Java Exercises 
### Reading from redirected `System.in` 

    Write a program that reads all lines from `System.in` and prints them. Test it by 
    piping a file to it from the command line. 
    
    
