## Java Exercises 
### `remove(int)` vs `remove(Object)` 

    Create an `ArrayList` of Integers. Demonstrate the difference between calling `remove(2)` 
    and `remove(Integer.valueOf(2))`. 
    
    
