## Java Exercises 
### Reverse DNS Lookup with InetAddress 

    Write a utility that takes an IP address as input and uses 'InetAddress.getByName().getHostName()' 
    to perform a reverse DNS lookup and print the corresponding hostname. 
    
    
    
