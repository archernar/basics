## Java Exercises 
### `exit` vs. `return` 

    Create a `main` method and a helper method. Show how `return` from `main` ends the 
    program, and contrast it with `System.exit()` from the helper method. 
    
    
