## Java Exercises 
### Annotation-Driven Data Validator 

    Create a validation framework that uses custom annotations. Define annotations like 
    '@NotNull', '@MinLength(5)', and '@Email'. Write a 'Validator' class that uses 
    reflection to check an object's fields against these annotations and reports any 
    violations. 
    
    
