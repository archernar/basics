## Java Exercises 
### Build a Rate Limiter 

    Implement a token bucket algorithm to create a rate limiter. The 'RateLimiter' class 
    should have a method like 'tryAcquire()' that returns true if a request is allowed 
    and false if it exceeds the configured rate (e.g., 10 requests per second). 
    
    
    
