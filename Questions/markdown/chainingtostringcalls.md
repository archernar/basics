## Java Exercises 
### Chaining toString() Calls 

    Create a 'Person' class with a 'toString()' method. Create an 'Employee' subclass 
    that extends 'Person'. The 'Employee' 'toString()' method should call 'super.toString()' 
    and append additional employee-specific information (like employee ID). 
    
    
    
