## Java Exercises 
### Using the `super` Keyword with Constructors 

    Create a `Machine` class with a constructor that accepts a name. The `Computer` subclass 
    constructor should call the parent constructor using `super()`. 
    
    
