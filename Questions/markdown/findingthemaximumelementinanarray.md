## Java Exercises 
### Finding the Maximum Element in an Array 

    Write a recursive function 'findMax(int[] arr, int index)' that finds and returns 
    the largest integer in an array. The recursion should compare the element at the 
    current index with the maximum of the rest of the array. 
    
    
