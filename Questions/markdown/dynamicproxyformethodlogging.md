## Java Exercises 
### Dynamic Proxy for Method Logging 

    Use Java's 'java.lang.reflect.Proxy' class to create a dynamic proxy for an interface. 
    The proxy's 'InvocationHandler' should log every method call on the target 
    object, including method name and arguments, before delegating the call to the 
    original object. 
    
    
