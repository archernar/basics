## Java Exercises 
### Character Wrapper Methods 

    Use the Character wrapper class to check if a character is a digit (isDigit), a letter 
    (isLetter), or whitespace (isWhitespace). 
    
    
