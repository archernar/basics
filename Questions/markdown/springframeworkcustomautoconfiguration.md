## Java Exercises 
### Spring Framework: Custom Autoconfiguration 

    For users of the Spring Framework, create a custom 'starter' module. The module should 
    use Spring Boot's autoconfiguration mechanism to automatically configure a 
    specific bean or set of beans when it's included as a dependency. 
    
    
