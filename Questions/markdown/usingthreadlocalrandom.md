## Java Exercises 
### Using ThreadLocalRandom 

    Use `ThreadLocalRandom.current().nextInt(min, max)` to generate a random integer 
    in a user-defined range. Note this method's upper bound is exclusive. 
    
    
