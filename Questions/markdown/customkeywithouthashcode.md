## Java Exercises 
### Custom Key - Without `hashCode()` 

    Demonstrate what happens when you use a custom object as a key without overriding 
    `hashCode()` (hint: `get()` will likely fail). 
    
    
