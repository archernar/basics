## Java Exercises 
### 'join()' with a Timeout 

    Start a worker thread that sleeps for 5 seconds. The main thread should call 'worker.join(2000)' 
    (a 2-second timeout). The main thread should then print whether 
    the worker is still alive ('isAlive()') to show that the join timed out. 
    
    
