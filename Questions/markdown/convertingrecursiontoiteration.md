## Java Exercises 
### Converting Recursion to Iteration 

    Take the recursive factorial function. Rewrite it to use an explicit stack ('java.util.Stack') 
    instead of the call stack to simulate the recursion, effectively converting 
    it to an iterative solution. 
    
    
