## Java Exercises 
### In toString() Method 

    When overriding the 'toString()' method, use 'this' implicitly or explicitly to access 
    the instance variables to build the string representation. 
    
    
