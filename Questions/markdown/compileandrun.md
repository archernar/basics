## Java Exercises 
### Compile and Run 

    Using the command line (like Terminal or Command Prompt), compile your `Hello.java` 
    file using `javac` and run the resulting `.class` file using `java`. 
    
    
