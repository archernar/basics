## Java Exercises 
### Widening vs. Varargs Priority 

    Create a class with methods 'process(int a, long b)' and 'process(int... nums)'. 
    Call the method with two 'int' arguments, e.g., 'process(5, 10)'. Explain why the 
    'long' version is chosen over the varargs version. 
    
    
