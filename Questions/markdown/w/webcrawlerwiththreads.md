## Java Exercises 
### Web Crawler with Threads 

    Build a simple web crawler. Start with a single URL. One or more threads should download 
    the page, parse it for new URLs, and add those to a shared, thread-safe 
    queue of URLs to be visited. The process repeats until the queue is empty or a depth 
    limit is reached. 
    
    
