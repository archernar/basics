## Java Exercises 
### Wrapper Class Methods 

    Use the Integer wrapper class to convert a string to an int (parseInt), an int to 
    a string (toString), and to find the min/max of two numbers (min, max). 
    
    
