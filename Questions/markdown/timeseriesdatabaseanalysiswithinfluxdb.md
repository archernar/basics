## Java Exercises 
### Time-Series Database Analysis with InfluxDB 

    Set up an InfluxDB instance. Write a Java application that pushes time-series data 
    (e.g., mock CPU and memory metrics) to the database using the InfluxDB Java client. 
    Then, write queries to aggregate and analyze the data over time windows. 
    
    
