## Java Exercises 
### Stream Method - ints() - Infinite 

    Use the `ints()` method without arguments to get an infinite stream of integers, 
    and use `limit(5)` to take the first five. 
    
    
