## Java Exercises 
### Basic 'Hello World' Server 

    Write a TCP server using 'ServerSocket' that waits for a single client connection. 
    When a client connects, the server sends the string 'Hello, client!' and then 
    closes the connection. 
    
    
