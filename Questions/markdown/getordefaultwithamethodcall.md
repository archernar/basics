## Java Exercises 
### `getOrDefault` with a method call 

    Use a method call as the default value in `getOrDefault`, and show that the method 
    is only called if the key is not present. 
    
    
