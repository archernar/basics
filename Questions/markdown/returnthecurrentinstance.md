## Java Exercises 
### Return the Current Instance 

    Create a method with a fluent interface style that returns 'this' (the current object) 
    to allow for method chaining (e.g. object.setX(10).setY(20);). 
    
    
