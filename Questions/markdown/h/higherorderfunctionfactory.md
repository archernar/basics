## Java Exercises 
### Higher-Order Function Factory 

    Create a class that contains methods which return other functions (i.e., 'java.util.function.Function' 
    or similar). For example, a method 'createMultiplier(int factor)' 
    that returns a lambda expression which multiplies its input by the given 
    factor. 
    
    
