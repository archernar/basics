## Java Exercises 
### Hiding Complexity 

    Create a class that connects to a database. The public method might be 'getUser(id)', 
    but the private interface would consist of methods like 'openConnection()', 
    'buildQuery()', 'executeQuery()', and 'closeConnection()'. 
    
    
