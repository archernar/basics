## Java Exercises 
### Custom Key - `equals()` and `hashCode()` 

    Create a custom class for a key. Implement `equals()` and `hashCode()` correctly 
    and use it as a key in a `HashMap`. 
    
    
