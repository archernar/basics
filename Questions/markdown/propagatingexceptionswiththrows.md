## Java Exercises 
### Propagating Exceptions with 'throws' 

    Create a method 'readFile(String fileName)' that might throw an 'IOException'. Instead 
    of catching it, use the 'throws' keyword in the method signature to propagate 
    it to the caller. 
    
    
