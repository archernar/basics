## Java Exercises 
### Exception in an Enumeration 

    Create an 'enum' with a constructor that can throw an exception. Show what happens 
    when the JVM tries to initialize this enum type and the constructor fails. 
    
    
