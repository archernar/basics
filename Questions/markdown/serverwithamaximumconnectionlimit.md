## Java Exercises 
### Server with a Maximum Connection Limit 

    Modify the thread pool server to enforce a limit on the number of concurrent client 
    connections. If the limit is reached, the server should politely refuse new connections 
    with a 'Server busy' message. 
    
    
