## Java Exercises 
### Static Methods Cannot Be Overridden 

    Create a class with a static method. Create a subclass with a static method of the 
    same signature. Show that this is method hiding, not overriding, by calling the 
    method on references of both the superclass and subclass types. 
    
    
