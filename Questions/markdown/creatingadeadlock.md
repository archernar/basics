## Java Exercises 
### Creating a Deadlock 

    Write a program where two threads each attempt to acquire two locks in opposite order. 
    Thread A locks Lock1 then Lock2. Thread B locks Lock2 then Lock1. Run the 
    program to demonstrate a deadlock, then use 'jstack' to analyze it. 
    
    
