## Java Exercises 
### Grouping Threads with 'ThreadGroup' 

    Create a 'ThreadGroup'. Instantiate several threads, passing the group to their constructors. 
    Write code that uses the 'ThreadGroup' to interrupt all threads in 
    the group with a single method call. 
    
    
