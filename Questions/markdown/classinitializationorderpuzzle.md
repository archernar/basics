## Java Exercises 
### Class Initialization Order Puzzle 

    Create a class hierarchy with static blocks, instance initializers, and constructors 
    in both the superclass and subclass. Write a 'main' method that instantiates 
    the subclass and predict the exact order of output, explaining the rules of Java 
    class initialization. 
    
    
