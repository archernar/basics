## Java Exercises 
### Difference between `currentTimeMillis` and `nanoTime` 

    In a comment, explain the difference between `System.currentTimeMillis()` for wall-clock 
    time and `System.nanoTime()` for measuring elapsed time. 
    
    
