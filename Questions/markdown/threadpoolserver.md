## Java Exercises 
### Thread Pool Server 

    Improve the multithreaded server by using an 'ExecutorService' (a thread pool). Instead 
    of creating a new thread for each client, submit a 'Runnable' task to the 
    pool to handle the client socket. 
    
    
