## Java Exercises 
### Getter/Setter for an Array 

    In a class with a private array field, the getter could return a copy of the array. 
    Discuss the design choices for a setter (e.g. set the whole array, or provide 
    'add'/'remove' methods instead). 
    
    
