## Java Exercises 
### Configuring TCP_NODELAY 

    Write a client-server application that sends many small, frequent messages. Measure 
    the latency. Then, enable 'socket.setTcpNoDelay(true)' on both ends and measure 
    again to observe the effect of disabling Nagle's algorithm. 
    
    
