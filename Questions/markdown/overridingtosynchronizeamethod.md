## Java Exercises 
### Overriding to Synchronize a Method 

    Create a 'Counter' class with a method 'increment()'. Create a 'SynchronizedCounter' 
    subclass that extends 'Counter' and overrides the 'increment()' method. The 
    overriding method should add the 'synchronized' keyword to make it thread-safe. 
    
    
    
