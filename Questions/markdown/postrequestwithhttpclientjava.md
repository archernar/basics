## Java Exercises 
### POST Request with HttpClient (Java 11+) 

    Use the 'HttpClient' to send a POST request to a test API endpoint (e.g., 'https://jsonplaceholder.typicode.com/posts'). 
    Include a JSON string as the request body 
    and set the 'Content-Type' header to 'application/json'. 
    
    
