## Java Exercises 
### Implicit super() Call 

    Create a superclass with a no-arg constructor and a subclass with a constructor. 
    Show that 'super()' is called implicitly if not written explicitly. 
    
    
