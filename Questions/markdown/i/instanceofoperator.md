## Java Exercises 
### InstanceOfOperator 

    In your polymorphic 'Vehicle' array, use the 'instanceof' operator to check if an 
    object is a 'Car' before calling a 'Car'-specific method (e.g. 'openTrunk()'). 
    
    
    
