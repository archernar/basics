## Java Exercises 
### Iterating Over Entries 

    Use a `for-each` loop with `entrySet()` to iterate through the HashMap and print 
    each entry in the format 'Key: [key], Value: [value]'. 
    
    
