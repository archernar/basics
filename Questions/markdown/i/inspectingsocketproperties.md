## Java Exercises 
### Inspecting Socket Properties 

    Write a program that establishes a socket connection and then uses methods like 'getInetAddress()', 
    'getPort()', 'getLocalAddress()', and 'getLocalPort()' to print 
    details about both the local and remote endpoints of the connection. 
    
    
