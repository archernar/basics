## Java Exercises 
### ImmutableReferenceString 

    Create a 'String s = 'Start';'. Call 's.toUpperCase();'. Print 's' to show the original 
    object is unchanged because String is immutable. 
    
    
