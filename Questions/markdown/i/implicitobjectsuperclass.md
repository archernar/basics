## Java Exercises 
### Implicit `Object` Superclass 

    Create a simple `Person` class. Without extending anything explicitly, create an 
    instance and call methods like `hashCode()` and `getClass()` to show it inherits 
    from `Object`. 
    
    
