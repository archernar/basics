## Java Exercises 
### Intern a String 

    Create two strings with the same literal value and one with `new String()`. Use `intern()` 
    and `==` to show how it affects the string pool. 
    
    
