## Java Exercises 
### Accessing Parent Class Members 

    Create a `Vehicle` class with a `speed` field. Create a `Car` subclass that sets 
    and gets the `speed` field from the `Vehicle` class. 
    
    
