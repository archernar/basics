## Java Exercises 
### Combining Methods - Seeded Gaussian 

    Use a seeded constructor, then call `nextGaussian()` multiple times to demonstrate 
    it produces a predictable sequence of normally distributed numbers. 
    
    
