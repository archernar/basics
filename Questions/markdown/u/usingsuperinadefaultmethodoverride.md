## Java Exercises 
### Using super in a Default Method Override 

    Create an interface 'A' with a default method 'hello()'. Create an interface 'B' 
    that extends 'A' and provides its own default override of 'hello()', which calls 
    'A.super.hello()' and adds more functionality. 
    
    
