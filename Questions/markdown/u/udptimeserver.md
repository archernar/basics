## Java Exercises 
### UDP Time Server 

    Create a UDP server that responds to incoming packets by sending back the current 
    date and time as a string. The client just needs to send an empty packet to request 
    the time. 
    
    
