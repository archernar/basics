## Java Exercises 
### Virtual Threads vs. Platform Threads 

    Write a benchmark that creates a large number of tasks that perform a blocking I/O 
    operation (e.g., a network call or 'Thread.sleep()'). Run the benchmark once using 
    traditional platform threads and again using virtual threads (Project Loom). 
    Compare the memory usage and throughput. 
    
    
