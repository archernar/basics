## Java Exercises 
### Volatile Keyword Demonstration 

    Create a shared 'boolean flag' variable. One thread sets the flag to 'true' after 
    a delay. Another thread loops until it sees the flag as 'true'. First, run without 
    'volatile' and observe it may loop forever. Then, add 'volatile' and show that 
    the change is correctly visible. 
    
    
