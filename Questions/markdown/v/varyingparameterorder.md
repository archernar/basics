## Java Exercises 
### Varying Parameter Order 

    Create a 'Message' class with two overloaded methods named 'send'. One should have 
    the signature 'send(String recipient, String message)', and the other should be 
    'send(String message, int recipientId)'. Both should print the details of the 
    message being sent. 
    
    
