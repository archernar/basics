## Java Exercises 
### Environment variable case sensitivity 

    Check if `System.getenv()` is case-sensitive on your OS by trying to get a variable 
    with different casings (e.g., `PATH` vs `path`). 
    
    
