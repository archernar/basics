## Java Exercises 
### Compare performance of `+` vs `StringBuilder` 

    Repeat the previous exercise, but compare the `+` operator with using a `StringBuilder`'s 
    `append()` method. 
    
    
