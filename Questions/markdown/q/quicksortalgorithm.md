## Java Exercises 
### Quick Sort Algorithm 

    Implement the Quick Sort algorithm. Write a recursive function 'quickSort(int[] arr, 
    int low, int high)' that selects a pivot, partitions the array, and then recursively 
    sorts the sub-arrays. 
    
    
