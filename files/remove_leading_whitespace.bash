function remove_leading_whitespace() {
  local output="${input#"${input%%[![:space:]]*}"}"
  echo "$output"
}
function remove_leading_whitespace() {
  local output="${input#"${input%%[![:space:]]*}"}"
  echo "$output"
}
function remove_leading_whitespace() {
  local output="${input#"${input%%[![:space:]]*}"}"
  echo "$output"
}
function remove_leading_whitespace() {
  local output="${input#"${input%%[![:space:]]*}"}"
  echo "$output"
}
function remove_leading_whitespace() {
  local output="${input#"${input%%[![:space:]]*}"}"
  echo "$output"
}
function remove_leading_whitespace() {
  local output="${input#"${input%%[![:space:]]*}"}"
  echo "$output"
}
function remove_leading_whitespace() {
  local output="${input#"${input%%[![:space:]]*}"}"
  echo "$output"
}
function remove_leading_whitespace() {
  local output="${input#"${input%%[![:space:]]*}"}"
  echo "$output"
}
