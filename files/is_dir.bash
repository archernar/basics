function is_dir() {
  [[ -d "$1" ]]
}
function is_dir() {
  [[ -d "$1" ]]
}
function is_dir() {
  [[ -d "$1" ]]
}
function is_dir() {
  [[ -d "$1" ]]
}
function is_dir() {
  [[ -d "$1" ]]
}
function is_dir() {
  [[ -d "$1" ]]
}
function is_dir() {
  [[ -d "$1" ]]
}
function is_dir() {
  [[ -d "$1" ]]
}
