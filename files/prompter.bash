function prompter() {
  read -p "$question (default: $default): " response
  if [ -z "$response" ]; then
    echo "$default"
  else
    echo "$response"
  fi
}
function prompter() {
  read -p "$question (default: $default): " response
  if [ -z "$response" ]; then
    echo "$default"
  else
    echo "$response"
  fi
}
function prompter() {
  read -p "$question (default: $default): " response
  if [ -z "$response" ]; then
    echo "$default"
  else
    echo "$response"
  fi
}
