function collapse_hostname() {
    curr_hostname=$(hostname)
    echo $curr_hostname
}
function collapse_hostname() {
    curr_hostname=$(hostname)
    echo $curr_hostname
}
function collapse_hostname() {
    curr_hostname=$(hostname)
    echo $curr_hostname
}
function collapse_hostname() {
    curr_hostname=$(hostname)
    echo $curr_hostname
}
function collapse_hostname() {
    curr_hostname=$(hostname)
    echo $curr_hostname
}
function collapse_hostname() {
    curr_hostname=$(hostname)
    echo $curr_hostname
}
function collapse_hostname() {
    curr_hostname=$(hostname)
    echo $curr_hostname
}
function collapse_hostname() {
    curr_hostname=$(hostname)
    echo $curr_hostname
}
