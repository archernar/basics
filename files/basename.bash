function basename() {
  printf "%s\n" "$(basename "$path")"
}
function basename() {
  printf "%s\n" "$(basename "$path")"
}
function basename() {
  printf "%s\n" "$(basename "$path")"
}
function basename() {
  printf "%s\n" "$(basename "$path")"
}
function basename() {
  printf "%s\n" "$(basename "$path")"
}
function basename() {
  printf "%s\n" "$(basename "$path")"
}
function basename() {
  printf "%s\n" "$(basename "$path")"
}
function basename() {
  printf "%s\n" "$(basename "$path")"
}
function basename() {
  printf "%s\n" "$(basename "$path")"
}
